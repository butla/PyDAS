[tox]
envlist = py34
skipsdist = true

[testenv]
deps =
    -rrequirements.txt
    pytest
    pytest-cov
    #pylint
    docker-py
    git+git://github.com/butla/mountepy.git
    pipdeptree
commands =
    py.test --cov=mountepy --cov-report term --cov-report xml tests/
    #/bin/bash -c "pylint data_acquisition; exit 0"
